services:
  collector:
    build: .
    ports:
      - "8300:8300"
    volumes:
      - ./data:/data
    environment:
      TELEMETRY_DATA_DIR: /data
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
