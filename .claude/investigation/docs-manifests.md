# Discovery dossier — KEY=docs-manifests (Documentation & Manifests)

> **FROZEN EVIDENCE** — snapshot at commit `f6963c77` (2026-07-03, v0.12.7); deliberately unmaintained and expected to drift.
> Authority order: **live repo → `.claude/skills/` → this file (leads only — re-verify before citing).** See [`README.md`](./README.md).

Repo: Flowfile (branch `improvement/improve-naming-unnamed-flows`, clean)
Date: 2026-07-03. All claims verified by reading files / running read-only commands unless marked **inferred**.

---

## 1. Documentation inventory (what exists)

### Root-level docs
| File | What it is |
|---|---|
| `README.md` (232 lines) | GitHub landing page. Tagline "Visual ETL that compiles to Polars." Badges, demo link (demo.flowfile.org), feature tour with GIFs from `.github/images/`, quick starts (browser / `flowfile-editor` npm / pip / Python API), collapsed Docker & from-source sections, 3-service architecture sketch, TODO checklist, MIT license, acknowledgments. Claims **"40+ node types"** (line 52) and links a dev.to architecture deep-dive. |
| `readme-pypi.md` | PyPI long-description variant. Claims **"30+ nodes"** (drift vs README's 40+). Links per-package READMEs. |
| `CONTRIBUTING.md` (159 lines) | The contributor rulebook — full extraction in §4. |
| `CLAUDE.md` | Monorepo agent guide (assessed in §7). |
| `LICENSE` | MIT. |
| `mkdocs.yml` | Docs-site config (see §2). |

### docs/ tree (~70 markdown pages + assets)
Full listing captured 2026-07-03. Structure (mirrors `mkdocs.yml` nav exactly):

- `docs/index.html` — the site **Home is a hand-written HTML page** (965 lines, inline CSS with Material-theme CSS variables, teal/purple palette `#00CED1`/`#6B46C1`), not markdown. `nav: - Home: index.html`.
- `docs/quickstart.md` (643 lines) — heavily inline-styled HTML (gradient cards, clamp() typography, emoji checklists); dual-path (non-technical visual tutorial / technical Python API tutorial).
- `docs/community.md` (42 lines) — Discussions-vs-Issues routing table, ways to contribute, Contributor Covenant.
- `docs/users/` — user guides:
  - `index.md` — "Choose Your Path" (Visual Editor vs Python API), inline-styled grid.
  - `formulas/index.md` + `formulas/functions.md` — **functions.md is AUTO-GENERATED** (header: `<!-- AUTO-GENERATED by tools/generate_formula_docs.py — do not edit. Run 'make formula_docs'. -->`), 95 functions in 5 categories, card grid with playground deep-links; styling/behavior in `docs/stylesheets/formula-reference.css` + `docs/javascripts/formula-reference.js`.
  - `visual-editor/` — index, building-flows, `nodes/` (input/transform/combine/aggregate/output/ml + index), `catalog/` (index, virtual-tables, visualizations, schedules, sql-editor, secrets), kernels.md, node-designer.md, connections.md, settings.md, `tutorials/` (database-connectivity, cloud-connections, code-generator).
  - `python-api/` — index, quickstart, `concepts/` (design-concepts, expressions, formulas), `reference/` (reading-data, writing-data, catalog-references, data-types, flowframe-operations, aggregations, joins, cloud-connections, visual-ui), `tutorials/flowfile_frame_api.md`.
  - `deployment/` — index (comparison table), lite.md (Flowfile Lite browser edition, "**18 nodes** across 5 categories"), desktop.md, python.md, docker.md.
- `docs/ai/` — index.md (feature catalog: Chat, Agent with 3 variants Live-REPL/Staged/Single-shot-full, Auto intent routing, Cmd+K palette, inline ✨ actions, Fix-with-AI, docs generation) + providers.md (BYOK setup).
- `docs/for-developers/` — index, design-philosophy (615 ln, "The Architecture Story"), architecture (241 ln, **stale**, see §5), flowfile-core (527 ln, guided FlowGraph/FlowNode/FlowDataEngine tour with runnable snippets), python-api-reference (mkdocstrings-driven), kernel-architecture (405 ln, incl. the load-bearing "Versioning & compatibility" section), ai-architecture (565 ln: module map, routes catalog, planner state machine, GraphDiff, prompt logging), visualizations (105 ln, Graphic Walker four-process picture), creating-custom-nodes (486 ln), custom-node-tutorial (582 ln, emoji-generator walkthrough), docker-deployment (220 ln, incl. "Group-Based Sharing (multi-user mode)" section).
- `docs/assets/` — images organized per-guide; `docs/assets/images/nodes/*.svg` node icons.
- **Vestigial Sphinx leftovers** (the real site is MkDocs): `docs/conf.py` (Sphinx config, `release = "0.1.2"`, autoapi over core+worker, sphinx_rtd_theme), `docs/MakeFile` (Sphinx makefile — note odd `MakeFile` casing), `docs/requirements.txt` (**0 bytes, empty**). None are used by the MkDocs pipeline.

### Package-level docs
- **CLAUDE.md exists in exactly 8 packages + root** (verified by find): root, `flowfile_core/`, `flowfile_worker/`, `flowfile_frame/`, `flowfile_frontend/`, `flowfile_scheduler/`, `flowfile_wasm/`, `kernel_runtime/`, `shared/`. (No CLAUDE.md in `flowfile/`, `test_utils/`, `tools/`, `build_backends/`.)
- **READMEs**: `flowfile_core/README.md`, `flowfile_worker/README.md`, `flowfile_frontend/README.md` ("Flowfile Designer"), `flowfile_frame/readme.md` (lowercase; quick-start + type-stub workflow), `flowfile_wasm/README.md` (npm consumer docs for the published `flowfile-editor` package), `kernel_runtime/README.md`, `flowfile/readme.md` (CLI). **No README** for `flowfile_scheduler/` or `shared/` (CLAUDE.md is their only doc).
- `tools/migrate/README.md` — documents the `.flowfile` **pickle→YAML migration** (pickle format used ≤ v0.4.1, YAML v0.5+); a historical format-break artifact.
- `tools/README.md` exists (sidecar staging etc. — listed in tools/ dir).

### .github
- `ISSUE_TEMPLATE/bug_report.md` — the **unmodified GitHub default** bug template (asks for "Smartphone (please complete the following information): Device: [e.g. iPhone6]" — nonsensical for a desktop ETL tool). No feature-request template, **no PULL_REQUEST_TEMPLATE anywhere** (verified by find), **no SECURITY.md anywhere** (root or .github) — the security-reporting policy lives only in CONTRIBUTING.md §"Reporting security issues" (private advisory form).
- `.github/codeql/` — **empty directory** (0 tracked files; `git ls-files .github/codeql/` → nothing).
- `.github/images/` — README assets.
- `.github/workflows/` — **15 workflow files** on this branch AND on `origin/main` (verified `git ls-tree origin/main`): the 12 in root CLAUDE.md's table **plus** `claude.yml` (@claude mention-triggered agent), `claude-pr-review.yml` (anthropics/claude-code-action@v1 review on every non-draft PR), and `codeql.yaml` (see §5 — root CLAUDE.md says it was removed; it wasn't).

---

## 2. How the docs site is built & deployed (verified)

- **Engine**: MkDocs + Material theme. Config `mkdocs.yml`:
  - `site_url: https://edwardvaneechoud.github.io/Flowfile/`, `repo_url` set.
  - `use_directory_urls: false` → pages publish as `.html` files (this is why CONTRIBUTING links `.../community.html`). Comment in file: "Explicitly set the URL style for consistency and planning".
  - Theme: Material, indigo/orange palette, light+dark via `prefers-color-scheme` toggles, Roboto/Source Code Pro, features: navigation.tabs/sections/top, search.suggest/highlight, content.code.copy.
  - Plugins: `search`, `mkdocstrings` (python handler, `show_root_heading`, `show_inherited_members`, `show_bases`, `griffe_pydantic` extension with `schema: true` → renders Pydantic Fields/Validators sections).
  - Markdown extensions: `attr_list`, `md_in_html`, `admonition`, `pymdownx.details`, `pymdownx.superfences` (mermaid custom fence), `footnotes`.
  - `extra_css`: stylesheets/extra.css + formula-reference.css; `extra_javascript`: javascripts/formula-reference.js.
- **Dependencies** come from the root Poetry **dev group** (pyproject.toml lines 110–122: `mkdocs ^1.6.1`, `mkdocs-material ^9.5.46`, `mkdocstrings ^0.30.0`, `mkdocstrings-python ^1.16.12`, `griffe >=1.0,<2`, `griffe-pydantic >=1.1.6,<1.3`). `docs/requirements.txt` is empty and unused.
- **CI**: `.github/workflows/documentation.yml`
  - Triggers: push/PR to main with paths `docs/**`, `mkdocs.yml`, `flowfile_frame/**/*.py`, `tools/generate_formula_docs.py`, the workflow itself; plus `workflow_dispatch`. `permissions: contents: write`.
  - **build** job: Python 3.11, `pip install poetry`, `poetry install --with dev`, **`make check_formula_docs`** (drift gate), `poetry run mkdocs build`, upload `site/` artifact (7-day retention).
  - **deploy** job (push-to-main only): `poetry run mkdocs gh-deploy --force` → gh-pages branch → GitHub Pages.
- **Formula reference generation pipeline** (Makefile lines 277–290):
  - `make formula_docs` → `poetry run python tools/generate_formula_docs.py` → writes `docs/users/formulas/functions.md` from **polars-expr-transformer docstrings** (the library is the source of truth; rerun after bumping the pin). Output is deterministic (no timestamps, alphabetical per category). Vendored playground example grammar + `TRY_OVERRIDES` keep the page in sync with the interactive playground at edwardvaneechoud.github.io/polars_expr_transformer/.
  - `make check_formula_docs` = `formula_docs` + `git diff --exit-code -- docs/users/formulas/functions.md`; on drift prints `ERROR: formula docs are out of sync. Run 'make formula_docs' and commit the result.`
- Verified commands:
  - `poetry run mkdocs build` (what CI runs; not run locally by me — needs dev group installed) — **inferred to work from CI config**.
  - `make check_formula_docs` — defined at Makefile:285.

---

## 3. House style (docs voice & conventions)

Two distinct registers coexist:

1. **Legacy/marketing register** (quickstart.md, users/index.md, nodes/index.md, python-api pages, design-philosophy.md): first-person-plural, enthusiastic, emoji headers (🎨 🐍 ✅ 🚀 🎉), **heavy inline-styled HTML** (gradient `<div>`s, `clamp()` font sizing, colored cards), `<details markdown="1">` collapsibles, bold-italic hype ("**powerful data pipelines with ease**"). design-philosophy.md is unusually personal/narrative ("This wasn't some grand plan… it was just about building what sounded _fun_") and even contains typos ("almost by accident,.").
2. **Modern/engineering register** (ai/index.md, deployment/lite.md + index.md, kernel-architecture.md, ai-architecture.md, visualizations.md, docker-deployment.md sharing section, community.md — all newer pages): tight declarative prose, Material admonitions (`!!! info`, `!!! tip`, `!!! note`), comparison tables, exact file paths and symbol names, explicit contracts ("There is no min/max gate and nothing that rejects…"), mermaid diagrams. This register matches CONTRIBUTING.md and the CLAUDE.md files.

Recent doc commits trend hard toward register 2 (git log docs/: LSP #550, projects #524, notebooks #538, sources #536, node docs #535, formula docs #508). **A skill author should write new docs in register 2.**

Package README register: feature-list marketing with 🚀 emoji headers (core/worker/frontend); wasm README is consumer-product docs for the npm package.

CLAUDE.md house template (all 8 packages, uniform): `# CLAUDE.md - <pkg>` → one-sentence role + "Package-specific notes; see the root /CLAUDE.md for monorepo-wide…" → sections **Role / Layout / Key patterns & conventions / Running & entry points / Testing / Gotchas / Key files**. Dense, imperative, bold-leads for load-bearing rules, exact symbols/line-level facts, no fluff. Root CLAUDE.md adds Comments policy: "Keep comments minimal… one short line at most. This applies to all languages."

---

## 4. Documented contributor rules (CONTRIBUTING.md, verbatim-adjacent)

Process:
- Small fixes → just open a PR. "Anything bigger — open a GitHub Discussion or an issue first so we can agree on the shape before you write code."
- Questions → Discussions (community.html).
- MIT: "By contributing, you agree that your contribution will be released under the same license."

Dev setup:
- Python `>=3.10, <3.14`, Node 20+, Poetry, Rust only for Tauri shell (`npm run dev:web` works without), Docker only for `kernel` marker; Linux extras: `libwebkit2gtk-4.1-dev`, `libgtk-3-dev`, `libayatana-appindicator3-dev`, `librsvg2-dev`.

Style:
- Ruff: line 120, py310 target, F/E/W/I/UP/B, double quotes. "Use **Polars**, not pandas."
- Import order: stdlib, third-party, first-party — CONTRIBUTING's list **omits `flowfile_scheduler`** (root pyproject/CLAUDE.md include it).
- Frontend: semicolons, 2-space, double quotes, 100-char, trailing commas, LF; Vue 3 Composition API + TS, Pinia, Element Plus; `@` → `src/renderer/app/`.

Tests:
- "Any code change that can be tested should come with a test."
- Commands: `poetry run pytest flowfile_core/tests` (worker/frame likewise), `make test_coverage`, `poetry run pytest -m kernel`. "Markers: `worker`, `core`, `kernel`." (**stale** — see §5).
- Playwright: `npx playwright install --with-deps chromium`; `npm run test:web` "needs backend + preview server running"; `make test_e2e`. "Tauri-shell E2E (via tauri-driver) is a follow-up."

Commits & PRs:
- Branch naming `fix/...`, `feat/...`, `docs/...` — "nothing strict".
- "Commit messages: short imperative subject… Explain the *why* in the body if it isn't obvious from the diff."
- "One logical change per PR. Smaller PRs get reviewed faster."
- "Fill in the PR description: what changed, why, and how you tested it. Screenshots or short clips help for UI changes."
- "CI must be green before merge. If a check is flaky, say so in the PR — don't just re-run silently."
- "Don't force-push to `main`. Releases build from it."

Things to avoid:
- No `master_key.txt` / `.env` / credentials; no pandas; "On Windows, Polars is pinned to `<=1.25.2` (build constraint) — don't bump it unilaterally" (**stale**, §5); "Don't add features or abstractions 'for later.' Keep changes scoped to the task."

Security: "**do not** open a public issue for security vulnerabilities" → GitHub private "Report a vulnerability" form. Code of conduct: Contributor Covenant v2.1.

---

## 5. Contradictions, staleness, doc-vs-code drift (verified each side)

### CONTRIBUTING.md vs code
1. **Windows Polars pin**: CONTRIBUTING.md:150 says Windows pins Polars `<=1.25.2`. Root `pyproject.toml:37` = `polars = ">=1.8.2, <1.40"` (single cross-platform pin, no platform marker); root CLAUDE.md explicitly says "the old Windows `<=1.25.2` ceiling was removed". **CONTRIBUTING is stale.**
2. **Pytest markers**: CONTRIBUTING.md:111 "Markers: worker, core, kernel." Actual (pyproject.toml:132–139): **6 markers** — `worker`, `core`, `kernel`, `docker_integration`, `kafka`, **`lsp`** ("Tests for the notebook LSP (Jedi) code-intelligence surface").
3. **Repo layout** (CONTRIBUTING §Repository layout) omits `flowfile_scheduler/`, `shared/`, `test_utils/`, `tools/`, `build_backends/`, `docker-remote/`.
4. Import-order first-party list omits `flowfile_scheduler`.

### Root CLAUDE.md vs code (the file otherwise presents as authoritative — these rot points matter)
5. **Version**: says "Version: 0.11.0"; `pyproject.toml:3` → `version = "0.12.7"`.
6. **Migrations**: says Alembic versions "currently 001–021"; actual `flowfile_core/flowfile_core/alembic/versions/` runs **001–028** (022 catalog_notebooks, 023 workspace_projects, 024 visualization_dashboard_uuids, 025 project_track_data_artifacts, 026 project_uniqueness, 027 notebook_files, 028 catalog_namespace_storage). flowfile_core/CLAUDE.md already references "migrations 023–026" (fresher than root).
7. **Workflows**: says "12 workflows" and "(A legacy `codeql.yaml` advanced workflow was removed…)". Actual: **15 files**, on this branch and `origin/main`, including `codeql.yaml`, `claude.yml`, `claude-pr-review.yml` (latter two absent from the table). **`codeql.yaml` exists and references `./.github/codeql/codeql-config.yml`, which is NOT tracked** (`.github/codeql/` is empty) — i.e. the "failed weekly on a missing config file" failure mode described in root CLAUDE.md is plausibly still live, and the removal claim is false for this tree. Last commit touching codeql.yaml: `3777c661 Migrate from Electron to Tauri (#462)`.
8. **Markers table** lists 5 of 6 (missing `lsp`).
9. **File sizes**: flow_graph.py actual **5977 lines / 261KB** (doc says 5349/~224KB); flow_frame.py **3538 / 145KB** (doc: 3414/~140KB); expr.py **1679** lines (doc: 1722). These rot fast — skills should avoid restating exact counts.
10. **WASM node count**: root says "(lightweight, 16 nodes)"; docs lite.md + deployment/index.md say "**18 nodes**"; README says "20+ nodes"; flowfile_wasm/CLAUDE.md says "**23 types across 5 categories** (21 + the 2 catalog nodes)… marketing copy says '20+'". Canvas.vue palette meanwhile also gained `available: false` teaser entries (database_reader, cloud_storage_reader, rest_api_reader, kafka_source, google_analytics_reader, window_functions, sql_query, python_script, fuzzy_match, graph_solver) linking to full-app docs. Four different numbers in four places.
11. Build-table omissions: Makefile also has `formula_docs`, `check_formula_docs`, `bump-version`, `check-version`, `install_e2e`, `clean_dmg_mounts` targets (verified in Makefile) not in root CLAUDE.md's table; `tools/` also holds `bump_version.py`, `check_version_sync.py`, `generate_formula_docs.py`, `sign_macos_sidecars.sh` beyond the two items root CLAUDE.md mentions.

### kernel_runtime/CLAUDE.md vs code (version-contract paragraph fully rotted)
12. Says images default `...:0.3.0` → actual `manager.py:38–40` = **`0.4.0`** for base/ml/lite. Says kernel pyproject `0.3.1` → actual **`0.4.0`**. Says runtime `__version__ = 0.2.2` → actual **`0.3.0`** (`kernel_runtime/kernel_runtime/__init__.py:1`). Says root app `0.11.0` → actual `0.12.7`. Notably `docs/for-developers/kernel-architecture.md` §Versioning (lines 89–120) **is current** (0.4.0 / 0.3.0) — the docs page is fresher than the CLAUDE.md here.

### docs vs code
13. `docs/for-developers/architecture.md:11` — "**Designer (Electron + Vue)**": stale since Tauri migration (#462). Same page's DAG section (line 51+) describes an **`EtlGraph`** class — grep confirms `EtlGraph` does not exist anywhere in `flowfile_core`; the class is `FlowGraph` (flow_graph.py:1039). Worker example says `get_context('spawn') # or 'fork' depending on OS/needs` — worker CLAUDE.md/code force spawn unconditionally.
14. Residual "Electron" references: `users/visual-editor/catalog/secrets.md:20` ("Desktop (Electron)… stored at `~/.config/flowfile/`"), mermaid labels in kernel-architecture.md:19 and design-philosophy.md:507, docker-deployment.md:154 ("dormant in the desktop/Electron app").
15. `docs/for-developers/index.md:36` — broken copy-paste: `git clone [https://github.com/edwardvaneechoud/Flowfile](https://github.com/edwardvaneechoud/Flowfile)` — markdown link syntax inside a bash code block.
16. `docs/conf.py` — Sphinx `release = "0.1.2"`; vestigial (site is MkDocs). `docs/MakeFile` likewise; `docs/requirements.txt` empty.
17. README "40+ node types" vs readme-pypi "30+ nodes" (and PyPI page also claims "DuckDB" DB connectivity — **inferred** unverified against connectors).
18. CONTRIBUTING `npx playwright install --with-deps chromium` vs root CLAUDE.md `npx playwright install chromium` (trivial but a copy-paste divergence).
19. `git branch` note: local `main` refname is **ambiguous** in this clone ("warning: refname 'main' is ambiguous") and local `main` tree is stale (only 8 workflows); use `origin/main` for ground truth comparisons.

---

## 6. Per-package CLAUDE.md gotchas — near-verbatim extraction (the gold)

### flowfile_core/CLAUDE.md
- "`flowfile_core/__init__.py` runs `validate_setup()` then `init_db()` **on import** — importing the package has side effects (DB file creation, Alembic schema migration, default-user seeding)." (Memory adds: set `FLOWFILE_SKIP_STARTUP_MIGRATION=1` for diagnostics.)
- "Don't bypass the worker offload for full datasets, and don't add eager litellm imports anywhere under `ai/` — both break invariants enforced by tests."
- "There is no package-level `pyproject.toml`; deps, scripts, ruff, and pytest config all live in the monorepo-root `pyproject.toml`."
- "Kernel / AI-local-model / Docker features need Docker available; absence surfaces as runtime errors, not import errors."
- Patterns: router order/prefixes in main.py are load-bearing (`ai_admin_router` mounts on `/system`, not `/ai`, so admins can flip the gate); "Keep the `ai` package litellm-import-free at module level" (every `import litellm` lazy; `ai/__init__.py` setdefaults `LITELLM_LOCAL_MODEL_COST_MAP=True`; eager import breaks `test_classification`/`test_dry_run` lazy-contract tests); secrets `$ffsec$1$<user_id>$<fernet_token>`, HKDF info=`user-<id>`; SHA-256 API-key hash is deliberate (CodeQL false positive); kernels allocate host ports 19000–19999; "Add schema changes as a new `alembic/versions/NNN_*.py`; never hand-edit existing migrations"; project git-tracking: "The projection hooks must never raise — failures are swallowed (logged) so a sync error never breaks the originating save"; DB stays runtime source of truth; `--run-flow` forces `OFFLOAD_TO_WORKER.set(False)`, drops `explore_data` nodes, `--run-id` required.

### flowfile_worker/CLAUDE.md
- "Adding a new `OperationType`: it must be both a literal in `models.OperationType` AND a function name on `funcs` — both `spawner.start_process` and `streaming._spawn_subprocess` do `getattr(funcs, operation)`."
- "`status_dict`, `PROCESS_MEMORY_USAGE`, and the `ProcessManager` map are plain in-memory dicts in the parent — task state does not survive a worker restart; `clear_task` is the only thing that removes the IPC file + state (and the Kafka `.offsets.json` sidecar)."
- "On WebSocket client disconnect mid-task the subprocess is **handed off** to a daemon `handle_task` thread (`_handoff_to_background`), not killed; `p`/`progress`/`error_message` are nulled so the `finally` block doesn't reap them."
- "Connectors do blocking network I/O; they MUST run in the subprocess (via `start_generic_process`), never inline in the async endpoint, or they block the event loop."
- "Viz sessions are long-lived spawned children with idle-TTL reaping (`VizSessionRegistry.IDLE_TTL_SECONDS = 300`…)."
- Patterns: all compute spawn-context; children re-import fresh so `configs.py` skips argparse for non-MainProcess and falls back to env defaults; subprocess signalling protocol = shared `Value("i")` progress (0→100, -1 error) + `Array("c", 1024)` error + `Queue(maxsize=1)` result; catalog reads ONLY via `catalog_reader.open_catalog_table`/`open_virtual_result` with `validate_catalog_path`; `secrets.py` must stay byte-for-byte in sync with core (`SECRET_FORMAT_PREFIX`, `KEY_DERIVATION_VERSION = b"flowfile-secrets-v1"`) "or decryption breaks"; `@pytest.mark.slow` used on viz tests but not declared in markers.

### flowfile_frame/CLAUDE.md
- "**Stub gate.** … After any public API change run `make stubs` (root) and commit; `make check_stubs` is the CI gate that fails on drift." Three generators; submodule generator emits every other `.pyi` **including `__init__.pyi`**; `make stubs` then runs `ruff --select F401 --fix`.
- "Don't break the `LazyFrame`/`DataFrame` aliases or Polars dtype re-exports in `__init__.py` — generated flow code depends on them."
- "**Source string is load-bearing.** … ensure `_repr_str` builds correct Polars code … A wrong string breaks graph execution silently."
- Two emission paths: prefer dedicated core node settings; `_add_polars_code` only for genuinely complex expressions.
- NOT standalone: imports flowfile_core directly, in-process, no HTTP. tests/conftest sets `TESTING=True` + MinIO connection `minio-flowframe-test` expecting `localhost:9000`. No `frame` pytest marker exists.

### flowfile_frontend/CLAUDE.md
- "`vite.config.mjs` sets `optimizeDeps.force: true` and `server.strictPort: true` — cold starts re-bundle deps (~5–15s) and the dev server fails (not falls back) if 8080 is taken, on purpose so Tauri's hard-coded `devUrl` never points at the wrong port."
- "ESLint here is the **legacy** `.eslintrc.js` (eslint 8…), not the root flat config. `npm run lint` only touches `src/**/*.{ts,vue}`."
- "Sidecar shutdown is a graceful ladder (HTTP `/shutdown` → SIGTERM → SIGKILL)…; sidecars also self-reap via `FLOWFILE_SUPERVISOR_PID` + `shared/parent_watcher.py` if the shell is hard-killed. macOS Cmd+Q/dock quit fires `RunEvent::Exit` (not `ExitRequested`), so `lib.rs` matches both."
- "The per-node settings loader is a **string-interpolated dynamic import** keyed by node-type folder…: renaming a node's settings dir/component breaks loading at runtime (logs `console.error`, no build error)."
- Patterns: path aliases in **three** places (vite.config.mjs, tsconfig.json, vitest.config.ts) — keep in sync; hash history router; ALL desktop-native calls through `lib/desktop.ts` (never import `@tauri-apps/*` in view code); new Tauri commands registered in `generate_handler!`; no JSX (React only as dynamic import inside Graphic Walker wrappers); AG Grid modular v31; GA OAuth uses system browser (Google blocks embedded webviews); desktop port scan `core = 63578 + k*2`, worker = core+1, injected via `window.__FLOWFILE_PORTS__`; vite proxy replicates FastAPI's absolute-307 trailing-slash redirects.

### flowfile_scheduler/CLAUDE.md
- "Do not import `flowfile_core` (or anything that transitively pulls it in) anywhere in this package — it breaks the 'core depends on scheduler' direction and the standalone/lightweight contract."
- "`models.py` is a compatibility shim only; add or change ORM columns in `shared/models.py`, not here."
- "`run_once` and `stop` both call `_release_lock`; never delete a lock you don't hold (the release is guarded by `holder_id`)."
- "Datetimes from the DB are stored naive-UTC; the engine `.replace(tzinfo=timezone.utc)` on read. Preserve this… or you'll get aware/naive arithmetic errors."
- "Cron correctness hinges on the `last_cron_slot` cursor being in naive local time — don't 'fix' it to UTC or advance it to `next_run` instead of `now`; both regress the DST/catch-up behavior the tests pin."
- Patterns: single-leader via `SchedulerLock` row id=1, takeover after `STALE_THRESHOLD = 90`s; on fire cursor advances to **now** (single catch-up fire, never backfill); `table_trigger` poll path is a safety net — primary push path is `CatalogService._fire_table_trigger_schedules`; `_maybe_launch` creates FlowRun **before** spawning; blocking DB work via `asyncio.to_thread`.

### flowfile_wasm/CLAUDE.md
- "Pyodide needs `SharedArrayBuffer`, so the page must send COOP/COEP headers… embedders must set them on their host page or Pyodide fails to load."
- "Pyodide is pinned to **v0.27.7** … the last release with Polars support; bumping it likely breaks `loadPackage(['polars', 'pydantic'])`."
- "**Execution is explicit-only — data runs ONLY when the user clicks Run.**… Selecting a node, opening the Settings or Table panel… must **never** call an execution path… Regression guard: `tests/unit/no-auto-run.test.ts`."
- Flat-namespace contract: bootstrap "**dumps the package's entire namespace** … into Pyodide globals"; CPython unit tests "**cannot** catch a broken browser namespace — the real-Pyodide smoke test (`tests/pyodide-smoke/smoke.cjs`, CI job `pyodide-smoke`) … is the guard".
- "Parquet never touches Python as parquet: the wasm polars wheel has parquet compiled out but IPC kept… `_clean_strings_for_export` (nodes_io.py) rebuilds String/Binary columns before IPC export — the wasm build panics ('capacity overflow') converting view-type columns…"
- Bundler-opaque dynamic import for parquet-wasm ("a literal https: `import()` in the published lib breaks webpack5/esbuild embedders — guarded by `tests/unit/parquet-bridge.test.ts`").
- `optimizeDeps.exclude: ['pyodide']` + include react/GW "are load-bearing"; pyodide is CDN-loaded, not an npm dep; micropip pins (`openpyxl==3.1.5`, `XlsxWriter==3.2.0`) "must match `tests/python/requirements.txt`"; `preview` is a legacy node type migrated to `explore_data` on load; "integration" suite does NOT boot real Pyodide.

### kernel_runtime/CLAUDE.md
- "`main.artifact_store` and persistence state are **module-level singletons**; tests must reset them (the autouse fixture does)."
- "Persistence reads env at lifespan-start, not import… `RECOVERY_MODE` (`lazy`/`eager`/`clear`; `none` is a deprecated alias for `clear` and is **destructive**)…"
- "The README says 'Two images' — there are actually **three** flavours (base/ml/lite); trust core's `manager.py`/`flavours.py` and the Dockerfile build args." (A documented intra-repo doc contradiction.)
- "No `VOLUME` directive in the Dockerfile by design — `/shared` is bind-mounted at runtime so an anonymous volume can't shadow the named mount in DinD."
- "Deserialization uses pickle/cloudpickle (RCE vector); acceptable only because the trust boundary is the user's own code already running in the kernel."
- "`publish_global` needs a `source_registration_id`; … if it's still `None` … it no-ops, prints a warning, and returns `-1`."
- Patterns: `flowfile_ctx` canonical, `flowfile` deprecated alias; per-flow namespace LRU (`MAX_NAMESPACES` default 20), `__name__="__main__"` so user classes cloudpickle; contextvars-scoped ctx (RuntimeError outside `/execute`); host→container path translation (`/catalog_tables` checked before `/shared`; DinD passes through); no flowfile_core/worker imports — `_perform_delta_write` mirrors `shared/delta_utils.py`; **version facts stale, see §5.12**.

### shared/CLAUDE.md
- "`crypto/` exists on disk but contains only stale `__pycache__/*.pyc` — there is NO tracked crypto source module. Don't import `shared.crypto`."
- "`rest_api/__init__.py` and `google_analytics/__init__.py` are empty — import submodules directly."
- "`get_database_url()` resolution order: `FLOWFILE_DB_PATH` env → `TESTING=True` (temp test DB) → default `database_directory/flowfile_catalog.db`."
- "`local_model_directory` and `ai_sessions_directory` are deliberately NOT created in `_ensure_directories` — don't add them to the eager mkdir list."
- "**IMPORT-ONLY-DOWNWARD.** Never add an import from `flowfile_core`/`_worker`/`_scheduler`/`_frame` here."
- "`storage` is a singleton instantiated at import… Importing `shared.storage_config` (or `shared`) has filesystem side effects."
- "**Kernel-exchange dirs MUST stay under the shared volume.**… Do not relocate these to a path Docker containers can't see."
- Wire-model split: `*_encrypted` fields decrypted only worker-side; worker subclasses attach decryption (rest_api/GA) or `decrypt_fn` injection (kafka). "Wire models stay logic-free."
- `kernel_runtime` does NOT import shared — it only shares the on-disk volume.

---

## 7. CLAUDE.md quality assessment (for the later revision phase)

All 9 files follow best practice (concise, imperative, gotcha-dense, consistent template, "see root for monorepo-wide" dedup). Ranking and issues:

- **Strong, current**: `flowfile_scheduler` (best-in-class: every gotcha is a named invariant with the *why* and the regression direction), `flowfile_worker`, `shared`, `flowfile_wasm` (longest but every line load-bearing), `flowfile_frontend`, `flowfile_frame`.
- **Strong with drift**: `flowfile_core` — content excellent; the "migrations 023–026" range will rot (027–028 already exist); mentions `routes/project.py` & `project/` consistent with code.
- **Strong but version-rotted**: `kernel_runtime` — the entire "Version contract" paragraph cites three version numbers that are ALL stale (0.3.0→0.4.0 image, 0.3.1→0.4.0 pyproject, 0.2.2→0.3.0 API); ironically it defers to `docs/for-developers/kernel-architecture.md`, which IS current. Fix: strip literal numbers, point at the three authoritative locations only.
- **Root CLAUDE.md**: most comprehensive, but carries the most stale absolutes — version 0.11.0 (→0.12.7), 12 workflows (→15; codeql.yaml/claude.yml/claude-pr-review.yml missing from table; "codeql.yaml was removed" is false), migrations 001–021 (→028), 5 markers (→6, missing `lsp`), file line counts (flow_graph 5349→5977 etc.), WASM "16 nodes" (docs say 18, wasm CLAUDE.md 23 types), Makefile table missing formula_docs/check_formula_docs/bump-version/check-version. **Systemic lesson: exact counts/versions in CLAUDE.md rot within weeks — prefer pointers ("see pyproject.toml") or ranges, or accept a recurring refresh chore.**
- CLAUDE.md provenance: bulk-added/refreshed in PR #486 ("update and add new claude.md files"), updated through #555 (object storage in catalog). `git log -- '*/CLAUDE.md'` shows they ARE maintained per-feature (RBAC #502, explicit-only execution #521, projects #524, notebooks #538) — the process exists but misses cross-cutting bumps (versions, counts).

---

## 8. Important knowledge that is UNdocumented (gaps a skill library should fill)

- **No PR template, no feature-request issue template**; the sole bug template is GitHub's untouched default (smartphone questions). CONTRIBUTING's PR-description expectations are the only guidance.
- **No SECURITY.md** — private-reporting policy buried in CONTRIBUTING.
- **`claude.yml` / `claude-pr-review.yml` workflows are documented nowhere** (root CLAUDE.md workflow table omits them) — every PR gets an automated Claude review; contributors aren't told.
- **Flow-in-flow feature (#568, merged) has zero docs** (grep for subflow/flow-in-flow in docs/ → nothing).
- **Project git-tracking** (`flowfile project init|open|save`, `FLOWFILE_ENABLE_PROJECTS`) has only a passing mention in `docs/users/deployment/docker.md`; no user guide page despite being a headline feature (#524).
- **Group-based sharing** has a section in for-developers/docker-deployment.md but no user-guide page; frontend mounts still pending per memory.
- **No developer docs for**: `flowfile_scheduler` internals (lock/cron semantics live only in its CLAUDE.md), `shared/` package contracts, `flowfile_worker` internals (subprocess protocol only in CLAUDE.md), `test_utils/` Docker fixtures, the `lsp` test surface / notebook LSP feature (#550 added docs? — `lsp` marker exists; docs coverage **inferred** thin, not verified page-by-page).
- **Formula-docs drift gate** (`make check_formula_docs` failing CI on unregenerated functions.md) is not mentioned in CONTRIBUTING or root CLAUDE.md — a newcomer bumping polars-expr-transformer will hit a red Documentation build with no signposting outside the Makefile comment.
- The **docs Home page being raw HTML** (docs/index.html) is undocumented; anyone "fixing" it to index.md changes the site root.
- `use_directory_urls: false` implies all cross-links use `.html` — adding pages with directory-URL-style links will 404.
- Sphinx leftovers (conf.py/MakeFile/empty requirements.txt) invite a newcomer to run the wrong build system.

---

## 9. Historical incidents (symptom → root cause → fix → status)

1. **CodeQL weekly failure** — symptom: scheduled CodeQL run fails; root cause: `codeql.yaml` references `.github/codeql/codeql-config.yml` which is untracked/absent; fix per root CLAUDE.md: switch to GitHub default setup and remove the workflow; **status: NOT done in-tree — codeql.yaml still present on origin/main and this branch; discrepancy live**.
2. **Kernel JSON global-artifact bug** — symptom: JSON-format global artifacts fail to deserialize; root cause: kernel image bug; fix: image `0.3.1`; surfaced as non-blocking "Update available" hint (kernel-architecture.md:109) because older images are never hard-blocked. Status: fixed; current pin 0.4.0.
3. **Pickle → YAML flow-file migration** — `.flowfile` was pickle ≤ v0.4.1, YAML v0.5+; `tools/migrate` README documents the converter. Status: tool maintained for legacy files.
4. **Electron → Tauri migration (#462)** — large rename; residue: stale "Electron" mentions in 5 docs pages (§5.14) and the codeql.yaml last-touch. Status: docs cleanup incomplete.
5. **WASM "capacity overflow" panic** — symptom: wasm build panics exporting IPC; root cause: view-type String/Binary columns from IPC import/excel reader; fix: `_clean_strings_for_export` rebuilds columns (wasm CLAUDE.md). Status: fixed, guard documented.
6. **Published-lib dynamic import breakage** — literal `https:` import() broke webpack5/esbuild embedders; fix: bundler-opaque dynamic import + `tests/unit/parquet-bridge.test.ts` guard. Status: fixed with regression test.
7. **Auto-run regression (#521)** — panel/selection actions used to trigger execution in WASM; fix: explicit-only execution doctrine + `tests/unit/no-auto-run.test.ts`. Status: enforced.
8. **macOS quit path** — Cmd+Q fires `RunEvent::Exit` not `ExitRequested`; sidecars leaked until lib.rs matched both + parent-death watcher added. Status: fixed; desktop E2E for it still a TODO (top of tests/web-flow.spec.ts).
9. **Worker queue-drain deadlock** — commit `3857aced` "fix(worker): drain result queue before joining child to avoid deadlock (#564)". Status: fixed.
10. From user memory (cross-session, not docs): importing flowfile_core auto-runs Alembic against the live catalog DB (bit once → `FLOWFILE_SKIP_STARTUP_MIGRATION=1`); shared fixed test DB dropped by concurrent pytest teardown → phantom "no such table" (→ `FLOWFILE_DB_PATH` isolation); trailing-slash 307s silently fail in Docker (fix frontend-side); repo-wide comment purge 2026-06-04 (lean comments doctrine).

---

## 10. Verified commands (ran or config-verified)

Ran (read-only):
- `find docs -type f | sort` — full docs tree (~70 md pages + assets).
- `find . -maxdepth 2 -name CLAUDE.md` — exactly 9 files (root + 8 packages).
- `grep -n "polars" pyproject.toml` — prints `polars = ">=1.8.2, <1.40"` at line 37.
- `ls flowfile_core/flowfile_core/alembic/versions/` — 001–028.
- `sed -n 130,142p pyproject.toml` — 6 pytest markers incl. `lsp`.
- `ls .github/workflows | wc -l` — 15.
- `git ls-tree origin/main -- .github/workflows/` — same 15 on main; `git ls-files .github/codeql/` — empty.
- `git log --oneline -- docs/` / `-- '*/CLAUDE.md'` — maintenance history (#486…#555).
- `grep -rn "EtlGraph" flowfile_core/.../flowfile_core --include='*.py'` — no hits; `class FlowGraph` at flow_graph.py:1039.
- `grep -n "_KERNEL_IMAGE_.*_DEFAULT" flowfile_core/flowfile_core/kernel/manager.py` — 0.4.0 tags at lines 38–40.
- `wc -l -c` on flow_graph.py / flow_frame.py / expr.py — 5977/3538/1679 lines.
- `head docs/users/formulas/functions.md` — AUTO-GENERATED header, 95 functions.

Documented-and-config-verified (not executed):
- `poetry run mkdocs build` and `poetry run mkdocs gh-deploy --force` — exactly what documentation.yml runs (build/deploy jobs).
- `make formula_docs` → `poetry run python tools/generate_formula_docs.py` (Makefile:277–282); `make check_formula_docs` → regenerate + `git diff --exit-code -- docs/users/formulas/functions.md` (Makefile:285–290).
- `poetry install --with dev` pulls mkdocs/mkdocstrings/griffe-pydantic (pyproject dev group lines 110–122).

---

## 11. Skill-library implications from this dimension

1. **Docs-authoring skill**: write in the modern register (Material admonitions, tables, exact paths, mermaid); remember Home is `index.html`, `use_directory_urls: false` (.html links), formula reference is generated (never hand-edit functions.md; run `make formula_docs`), docs deploy auto on main push via `mkdocs gh-deploy --force`, dev-group deps not docs/requirements.txt.
2. **CLAUDE.md maintenance skill**: the package template (Role/Layout/Patterns/Running/Testing/Gotchas/Key files) is house style; ban exact rot-prone literals (versions, counts, line numbers) in favor of authoritative-location pointers; the revision phase has a concrete stale-fact punch list (§5 items 5–12).
3. **Contributor-workflow skill**: encode CONTRIBUTING rules + the undocumented realities (Claude PR auto-review on every non-draft PR; formula-docs and stub drift gates; 6 markers incl. `lsp`; no PR template so the description convention is the contract).
4. **Ground-truth doctrine**: this repo's prose numbers disagree with each other constantly (node counts 16/18/20+/23/30+/40+; kernel versions; workflow counts) — skills must instruct agents to verify counts/versions from code, and prefer `origin/main` (local `main` refname ambiguous and stale in this clone).
5. **Per-package gotcha corpus** (§6) is the highest-value seed content — near-verbatim import into package skills is warranted; each was spot-verified where cheap (spawn context, marker list, stub targets, kernel ports, version pins).
6. **Doc-gap backlog** (§8) doubles as a "what not to assume is documented" list: flow-in-flow, projects, sharing UI, scheduler/shared/worker internals live only in CLAUDE.md files, not the docs site.
